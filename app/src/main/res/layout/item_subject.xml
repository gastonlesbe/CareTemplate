<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/ivIcon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/ic_header_flavor"
        android:contentDescription="@string/app_name"
        android:background="@drawable/gb_rounded_image"
         />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:layout_weight="1">

        <TextView
            android:id="@+id/tvSubjectName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/placeholder_name"
            style="@style/AppText.Subtitle" />

        <TextView
            android:id="@+id/tvSubjectInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/placeholder_age_weight"
            style="@style/AppText.Small"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginTop="2dp"/>

        <TextView
            android:id="@+id/tvSubjectExtra"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/placeholder_next_event"
            style="@style/AppText.Small"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginTop="2dp"/>
    </LinearLayout>

    <!-- Botones de acción: Editar y Borrar -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp">

        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_edit"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Editar"
            android:padding="8dp"
            android:tint="?attr/colorPrimary" />

        <ImageButton
            android:id="@+id/btnDelete"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_delete"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Borrar"
            android:padding="8dp"
            android:tint="?attr/colorError"
            android:layout_marginStart="4dp" />
    </LinearLayout>
</LinearLayout>
